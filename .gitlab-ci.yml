stages:
  - install
 
install_dependencies:
  stage: install
  image: node:20.14
  tags:
    - babel
  script:
    - node --version
  only:
    refs:
      - merge_requests
      - gdm
    changes:
      - yarn.lock